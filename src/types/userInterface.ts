export interface tokenPayloadInterface {
    id: number;
    name: string;
    email: string;
}

export interface UserInterface {
    id: number;
    name: string;
    email: string;
    password: string;
    createdAt: string;
}

